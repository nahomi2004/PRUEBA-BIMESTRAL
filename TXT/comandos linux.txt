we// DESCARGAR WORKBENCH
su

sudo apt Update
sudo apt install mysql-server
sudo systemctl status mysql
sudo MySQL --version

sudo mysql
ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'tuclave'; 
exit;


// DESCARGAR NET BEANS 

sudo apt update
sudo apt install -y openjdk-11-jdk
java -version (Verifica la version instalada)


sudo add-apt-repository "deb http://archive.ubuntu.com/ubuntu focal universe"
sudo apt update

instalan Apache NetBeans (Ubuntu Software)

instalar JDK17

sudo apt-get update
sudo apt-get upgrade
sudo apt install openjdk-17-jdk
sudo apt install openjdk-17-jre

verificar version

java --version
